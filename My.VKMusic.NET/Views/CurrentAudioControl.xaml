<UserControl x:Class="My.VKMusic.Views.CurrentAudioControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="400">
    <Grid>
        <StackPanel Margin="5" Orientation="Horizontal">
            <Label FontWeight="Bold" FontSize="16" Foreground="#2B587A"  Content="{Binding Info.Artist, FallbackValue=Artist}"/>
            <Label Content="-" FontSize="16"/>
            <Label FontSize="16" Content="{Binding Info.Title, FallbackValue=Title}"/>
        </StackPanel>
    </Grid>
</UserControl>
